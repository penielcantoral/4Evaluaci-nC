$scope.juego=function (){
	$scope.personaje-();
	var juego = $scope
	$scope.personaje.push({
		$scope.miembros.length + 1
	})
}
/run BNSetMatureLanguageFilter(true)
/run local x=GetAccountExpansionLevel(); SendChatMessage(x)